// ? These must be synced with DEFAULT_SETTIGNS in content folder

export const DEFAULT_SETTINGS = {
    extensionOn: true,
    askForRatingsAlarmLimit: 7, // * In days
    isDev: false,
    langDialect: "en-US",
    showMatchDiacritics: false,
    scrollSnap: false,
    lastMode: "normal",

    lastNav: true,
    lastMain:true,
    lastCode:true,

    lastMatchCase: true,
    lastMatchWhole:true,
    lastMatchDiacritics:true,

};