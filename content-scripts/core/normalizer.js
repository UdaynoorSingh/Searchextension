"use strict";

/// <reference path="../initializer.js" />


SearchExt.Normalizer = (function () {

    function normalize(text, options) {
        return text;
    }

    return {
        normalize
    };
})();
